@model IEnumerable<MIS421Capstone.Models.Day>

@{
    ViewData["Title"] = "Index";
}

<h1>Please don't say the n word please i find it very offensive please</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Journal)
            </th>
        </tr>

        <tr>
            <th>
                Breakfast
            </th>
        </tr>
            
        <tr>
            <th>
                Lunch
            </th>
        </tr>
            
        <tr>
            <th>
                Dinner
            </th>
        </tr>
            
        <tr>
            <th>
                Snacks
            </th>
        </tr>
            
        </t>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Journal)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Foods)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Foods)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Foods)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Foods)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
